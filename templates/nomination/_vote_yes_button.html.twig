{{ form_start(nomination.vote_yes_button) }}
    {{ form_errors(nomination.vote_yes_button) }}

    {{ form_row(nomination.vote_yes_button.nomination_id) }}

    <input type="submit" class="vote-yes btn" value="{{ button_label|default(nomination.yes_votes) }}" />
{{ form_end(nomination.vote_yes_button) }}
