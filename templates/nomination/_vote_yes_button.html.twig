{{ form_start(nomination.vote.yes) }}
    {{ form_errors(nomination.vote.yes) }}

    {{ form_row(nomination.vote.yes.nomination) }}

    <input type="submit" class="vote-yes btn" value="{{ button_label|default(nomination.yes_votes) }}" />
{{ form_end(nomination.vote.yes) }}
