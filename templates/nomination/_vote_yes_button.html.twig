{{ form_start(voteForms[nomination.id].yes) }}
    {{ form_errors(voteForms[nomination.id].yes) }}

    {{ form_row(voteForms[nomination.id].yes.nomination) }}

    <input type="submit" class="yes btn" value="{{ button_label|default(nomination.yesCount) }}" />
{{ form_end(voteForms[nomination.id].yes) }}
