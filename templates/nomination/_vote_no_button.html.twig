{{ form_start(voteForms[nomination.id].no) }}
    {{ form_errors(voteForms[nomination.id].no) }}

    {{ form_row(voteForms[nomination.id].no.nomination) }}

    <input type="submit" id="vote-{{nomination.id}}-no" class="vote-no btn" 
           aria-label="No: {{nomination.noCount}} votes"
           value="{{ button_label|default(nomination.noCount) }}" />
{{ form_end(voteForms[nomination.id].no) }}
