{{ form_start(voteForms[nomination.0.id].no) }}
    {{ form_errors(voteForms[nomination.0.id].no) }}

    {{ form_row(voteForms[nomination.0.id].no.nomination) }}

    <input type="submit" id="vote-{{nomination.0.id}}-no" class="vote-no btn" 
           aria-label="No: {{nomination.0.noCount}} votes"
           value="{{ button_label|default(nomination.0.noCount) }}" />
{{ form_end(voteForms[nomination.0.id].no) }}
