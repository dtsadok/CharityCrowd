{{ form_start(nomination.vote.no) }}
    {{ form_errors(nomination.vote.no) }}

    {{ form_row(nomination.vote.no.nomination) }}

    <input type="submit" class="vote-no btn" value="{{ button_label|default(nomination.no_votes) }}" />
{{ form_end(nomination.vote.no) }}
